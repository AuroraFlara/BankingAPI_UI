server.port=8180
server.error.include-message=always

# Database Configuration with Fallbacks
# Format: ${VARIABLE_NAME:DEFAULT_VALUE}
spring.datasource.url=jdbc:mysql://localhost:3306/bankingapp
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:2957979Tcl$!!}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate
spring.jpa.generate-ddl=true
spring.jpa.hibernate.ddl-auto=update
spring.main.allow-circular-references=true

# JWT Configuration with Fallbacks
# Hardcoding these defaults means you don't need 'export' or '.env'
# Run the command in your terminal:openssl rand -base64 64 | tr -d '\n' then copy the output and paste it as the default value for jwt.secret
jwt.secret=vi6AOBq6dgzx78+7trdxvar5ZCPwE63LK9WRozVfLJozrhgxNgpVIKlvckh+D9KArP7VlFFXbpfeqQrgIkuFyg==%
jwt.expiration=${JWT_EXPIRATION:86400000}
jwt.header=Authorization
jwt.prefix=Bearer

# Mail Configuration (Using your provided Gmail App Password)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=cheleatang777@gmail.com
spring.mail.password=igoz kqod oavw aomy
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Cache and Performance
spring.cache.type=caffeine
spring.cache.cache-names=idempotency
spring.jackson.deserialization.fail-on-unknown-properties=true

# Logging for Research Debugging
logging.level.org.springframework.web=DEBUG

# Geolocation API with fallbacks to prevent startup crash
geo.api.url=${GEO_API_URL:https://api.findip.net/}
geo.api.key=${GEO_API_KEY:your-placeholder-key}